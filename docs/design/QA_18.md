# 0.18 · Vanguard integrated playtest

## Foundry menu controls · 12 September 2026

Home control visuals rebuilt as reusable native plates, preserving real Button labels, callbacks, focus and tooltips. Normal/reduced main menu, Equipment focus, and Equipment screen inspected at 1600×900 using `tests/menu_style_capture.gd`; output in ignored `output/menu-foundry*.png`. Corrected an initial capture-only void-expression error before rerendering; final capture log has no engine errors. No bitmap generation or imported-asset changes. Shared button depth and disabled treatment preserve layout; no gameplay, save, or release artifact changes.

Final `scripts/check.ps1` passes all 36 suites, including layout/UI/skill visual and compatibility checks (`output/menu-foundry-check.log`). Human assessment of this new visual direction remains pending; no quality score increased. Windows test.2 remains the published pre-redesign build. Capture fixture disables persistence and audio and does not mutate collection or settings.

## Upgrade card key badges · 11 September 2026

Published Windows `v0.26.0-test.2` from `045adb7`; exported-pack smoke passes. ZIP size 102,236,518 bytes, SHA256 `395bfb69fe20dd256c99bb40834452ad9a4f30d2226edcf1bc1005790f6dd965`. ZIP/checksum uploaded and GitHub non-draft status verified. Previous test.1 preserved.

Added matching ability-key badges beside upgrade-card icons, plus explicit choice-key footer. Rendered `combo24_test.gd -- --render`: 25 checks pass; card badge/name/stat/milestone layout visually inspected (`output/upgrade24.png`). Final `scripts/check.ps1` passes all 36 suites (`output/card-key-check.log`). Presentation-only change, no new art assets or balance values. Preparing Windows test.2 so the distributed game includes the badges.

## Windows release preparation · 11 September 2026

Published and verified `v0.26.0-test.1` from `4c8d7d4` as a non-draft GitHub pre-release. ZIP size 102,236,320 bytes, SHA256 `135e585e6a89b16b959acebc31c35c7ba135cbc57dfaaa71cf41d331aa3df4cd`. GitHub reports both ZIP and checksum uploaded. Re-extracted final ZIP contains seven expected files; its EXE exits 0 in headless startup and its PCK passes the full assets/music/Practice smoke. No unexpected engine errors. Evidence: `output/release-zip-smoke.log`, `output/zip-exe-start.log`, `output/zip-exe-errors.log`.

Owner-authorized packaging only. All 36 suites pass (`output/release-check.log`). Exported Godot 4.7.2 Windows x86_64 release EXE + PCK; actual release EXE launches from its export directory, exits 0 after ten recorded frames, and displays the expected full main menu (`output/release-launch00000009.png`). No engine errors in launch output. Editor loaded the exported PCK independently of loose resources and passed isolated Practice/HUD/rocket/dynamic-icon/all-nine-MP3 checks. Release templates omit editor script/path overrides; early attempts using those were discarded, and documentation now distinguishes real EXE startup from detailed pack verification. Test helper disables persistence; no rewards granted.

Distribution includes optional desktop-shortcut VBS, manual shortcut instructions, source commit and Godot engine notices. No generated assets or gameplay changes. The optional helper has not been executed against the owner's desktop. Menu version remains historical 0.18; ZIP/tag identify 0.26.0-test.1. Private repository access is required for GitHub downloads; direct ZIP sharing remains available.

## Mosquito optimization and population · 11 September 2026

Final `scripts/check.ps1`: all 36 suites pass. Both refined compatibility probes and current Vanguard artificial-health soak finish without engine errors. No timing threshold is included in regression, to avoid machine-dependent failures.

Owner's 25-enemy lag report reproduced as a simulation CPU spike in `tests/mosquito_perf_probe.gd`: fixed seed, Practice terrain, 25 drones, 600 frames at 60Hz, with empty and 60-rocket threat fields. Before: empty mean 1760µs/p95 11184µs/max 19343µs; crowded mean 2203µs/p95 14255µs/max 22044µs. After: empty mean 898µs/p95 1358µs/max 1911µs; crowded mean 1349µs/p95 1913µs/max 2826µs. These sequential headless microbenchmarks measure Mosquito AI only, not rendered frame rate or a guarantee on other hardware. Raw ignored logs: `output/mosquito-perf-before.log` and `output/mosquito-perf-after.log`.

Optimization preserves 17 candidate headings and 100ms decisions, distributes initial phases across six frames, and uses conservative capsule-path checks to return clear endpoints directly. Near walls/arena boundaries it retains the old four-unit swept solver. The focused suite now has 21 checks, including parity over 500 deterministic candidate samples (skipping invalid origins), edge fallback, phase distribution, double new-rule HP and normal-spawn cap with explicit stress-spawn bypass. Existing W avoidance and tracking-gun collision tests pass. HP changes apply to `arsenal26`; no save migration needed.

Current artificial-health Level 1 soak completes at 505 seconds, entry levels 5/13/22, ending level 24 with 743 kills. It is reliability evidence only, not a balance verdict; this route does not stress 25 Mosquitoes. Player files and running game are untouched. No new bitmap assets or visual changes.

## Missile Frenzy rocket visual follow-up · 11 September 2026

Replaced the mini bolt drawing with the shared Q rocket hull/fins/exhaust at 75% scale. Normal/reduced screenshots inspected at combat size via `arsenal26_test.gd -- --render` (37 checks pass); all 36 suites pass in `scripts/check.ps1`. No simulation or balance changes, new bitmaps, save writes or human-game restart. Evidence: ignored `output/frenzy-rocket-render.log`, `output/frenzy-rocket-check.log` and refreshed `output/arsenal26*.png`.

## Arsenal and support enemies 0.26 · 11 September 2026

Final `scripts/check.ps1` passes all 36 suites, including 37 focused arsenal checks. Covered: 27 paced shots with sufficient energy, five energy each and low-energy rejection; preserved Q stocks; EMP normal-Q fallback; fixed E range/damage and faster recharge; four-charge storage/resume; strict spin buffer, queued E consumption/expiry and boundary rebound; Hatchery child cap; aura heal/speed/decay; full-circle laser. Initial compile/type error and coarse-timestep fixture issues were resolved. Interface defaults fixture now snapshots defaults before loading user settings, avoiding failures caused by saved cast preferences; no player settings changed. No engine errors waived in final regression.

Normal/reduced combat captures (`output/arsenal26*.png`) inspected at actual rendered scale: distinct native Hatchery/Uplink silhouettes, aura boundary, mini rocket and rotating hammer accent. These are code-native visuals; the module reuses the existing rocket icon. No generated asset provenance added or required. Render fixture disables persistence and audio; no human game restarted.

Both refined compatibility probes complete without engine errors. Current ordinary Vanguard policies still lose at 21.5/31.3/23.3 seconds, before the added specialists; these do not measure their fairness. Fixed-seed artificial-health Level 1 soak completes at 505.0 seconds, entry levels 5/13/22, ending level 24 with 743 kills, peak 28 enemies; p95 step 870µs, max 2303µs. Concurrent CPU timing is diagnostic only. Soak health is artificial, no permanent rewards written. Perfect equal-rank Barrage ceiling is roughly 110–124% R but requires 135 shot energy plus activation: not a proven sustainable rotation. Review energy starvation, support pressure and E/Flash chaining with the owner before claiming balance quality.

Specification: ARSENAL_SUPPORT_26. New-run flag preserves older checkpoint behavior. No fun rating raised from automation.

## Mosquito and gradual enrage 0.25 · 11 September 2026

Final `scripts/check.ps1` passes 35 suites, including 15 new Mosquito/gun/enrage checks. Actual projectile simulation confirms three total piercing hits, acquisition priority and a hit against a moving drone; the isolated W-zone avoidance and point-blank vulnerability checks pass. The initial generic enemy-cycle fixture caught the missing orbit-state label; fixed before the final full run. A percent-escaping error in the new tooltip was likewise fixed and rerendered. No engine errors waived. Existing legacy image-load/WAV exit warnings remain.

Native Mosquito silhouette inspected at combat scale in normal/reduced effects, with no decorative wings changing the 12-unit hurt radius. Removed obsolete fifth-shot HUD counter. Upgrade stat projections and offline balance reference now use the revised gun, without counting piercing as extra single-target DPS. No user game was restarted; no permanent rewards written.

Both refined compatibility probes complete without engine errors. Current ordinary Vanguard policies lose at 21.5/31.3/23.3 seconds (idle/active/basics), before the first Mosquito wave; they do not measure the enemy's fairness. Fixed-seed A0 artificial-health Level 1 soak wins at 484.0 simulated seconds, encounter entry levels 5/12/21, ending level 23 with 662 kills, peak 25 enemies, p95 step 1069µs/max 5535µs. Concurrent CPU timings are diagnostics, not a controlled speed comparison or human balance evidence. The earlier completed soak already used the final Mosquito movement; only the reported phase label subsequently changed.

Review the one-minute enrage damage slope and delayed added rings, gun reliability at acquisition boundaries, and whether overlapping casts meaningfully catch Mosquitoes. The dodge planner is a finite-direction short-horizon heuristic, not guaranteed mathematical optimal avoidance. It never grants invulnerability. High-frequency Levels 3/6 need direct human review.

## Combos, art and Conductor Practice 0.24 · 11 September 2026

Final full regression passes 34 suites, including 25 combo/Practice/capstone checks, 56 support/art, 46 Level progression and 188 review checks. The updated capstone fixture verifies the requested starting ability instead of the superseded Q-charge expectation. Tests cover real Practice button selection and Reset preservation, save type validation, failed-bank rollback and a future normal run starting with E. Existing WAV exit warnings remain; no engine errors are waived.

Rendered and inspected the compact upgrade screen with four-line Flash changes and gold/purple milestone offers. An initial Flash-card overlap was fixed before the final render. Conductor Practice, relay/beam geometry, terrain panels and energy/repair silhouettes inspected at 1600×900 in normal/reduced effects. Fixtures use Dummy audio, not a human audio verdict. Output captures remain ignored. No new bitmap masters were generated; existing icons retain the shared pixel shader.

Both refined compatibility probes complete. Current fixed-seed ordinary Vanguard policies lose at 19.8/37.3/22.6 seconds (idle/active/basics); these crude policies do not establish combo fairness. Artificial-health A0 Level 1 soak wins in 469.3 simulated seconds, encounter levels 5/13/22, ending level 23 with 658 kills; peak 24 enemies, p95 720µs, max 2565µs. Do not compare concurrent CPU measurements as a performance speedup or interpret artificial health as normal survivability. No permanent rewards are written by fixtures.

Remaining human review: spin coverage and buffer timing, Flash arrival positioning, Q fuel distance around walls, capstone interpretation and Conductor route usefulness. Conductor is a Practice-only prototype sharing several Vanguard systems, not a balanced campaign class. Boss 60/90–120-second calibration and encounter variants remain outside this slice; overload still starts at 180 seconds.

## Support and composable mastery art 0.23 · verified 11 September 2026

Full `scripts/check.ps1` passes 33 suites, including 56 support/art checks, 46 Level progression, 188 review, 65 Operation and 579 benchmark integrity checks. The support fixture exercises energy charging/transfer, full-energy retention, EMP, lack of healing/free casts/cooldown acceleration, destructible attraction, boss immunity, ranged specialists, old checkpoint acceptance and current new-run/Practice routing. All thirteen modern nodes resolve a reusable base and overhanging badge with hover details. Five generated source masters have validated opaque alpha, square dimensions and bounded 128px imports; provenance and hashes are in the asset manifest.

Rendered 1600×900 Mastery, badge catalog, 64/40/32 logical icon scales, shop and support constructs in normal/reduced effects with Dummy audio. Inspected full node names and ranks, small badge silhouettes, health/energy reserve distinction and anchor HP/radius. Captures are ignored under output/support23. This is visual inspection, not a new owner preference or audio verdict. Existing two WAV exit warnings remain; no engine errors waived.

Fixed-seed new-rule A0 artificial-health Level 1 soak wins in 481.9 simulated seconds, encounter entry levels 5/12/21, ending level 23 with 664 kills, peak 41 enemies, p95 step 792µs and maximum 3007µs. No permanent rewards written. Artificial health validates completion/performance, not fair difficulty. Anchor interrupts regular specialists' normal attack cycle while attracted; its 340-unit reach and durability need particular human review. Energy reserve's economy also needs direct play. Conductor and future grapple class remain design notes only.

Both refined compatibility probes finish without logged engine errors. Current ordinary Vanguard policies still lose at 19.8/28.3/26.3 seconds (idle/active/basics), before acquiring support modules; they do not measure these replacements' value. Final focused rerun after removing the obsolete Bulwark gun tooltip passes all 56 checks. No user game was restarted.

## Level progression 0.22 · verified 11 September 2026

Latest owner authorization and implemented scope are recorded in LEVEL_PROGRESSION_22. Full regression passes 32 suites, including the modern progression fixture, 188 review checks, 65 Operation checks and 579 benchmark integrity checks. Native 1600×900 Level selection, every camp tab, victory and normal/reduced combat renders pass with explicit Dummy audio; inspected mastery/equipment layouts fit and continuation stays visible. A small follow-up ensures upgrade cards reflect mastery energy reductions; final focused runs pass 46 progression and 188 review checks. Existing two WAV ObjectDB exit warnings remain; no logged engine errors waived.

New-rule fixed-seed A0 ordinary bots lose at 19.8 / 28.3 / 26.3 seconds (idle / active / basics), the same early outcomes as before. First-level artificial-health soak completes in 464.6 simulated seconds, arrival levels 5/12/21, ending at level 23 with 654 kills. Last-level soak completes in 666.9 seconds, arrivals 5/13/25, ending at level 26 with 3,258 kills, peak 180 enemies. Both now use A0 regardless of the historical soak caller's difficulty argument; report labels were corrected to actual simulation difficulty. These are reliability results, not normal-health fairness or a population progression distribution. Fixed seed/loot seed retained; no permanent rewards written.

Both refined compatibility probes complete without engine errors; 0.10 adaptive-basics policies win their three-stage fixtures for all three seeds, while 0.09 moving policies lose. Current balanced tier-one level-24 analytical reference is 247.5 DPS; Chapter/Level 8's HP quotient is 61.6 seconds, 123.2 at half delivery. Modules, cast conflicts, opening burst and pet/proc damage are excluded, so this is not proof of a legal 60-second kill. The positive human verdict remains the primary reason to avoid further broad core/boss retuning before review.

Save tests cover modern tree dependencies/counts/resume, old-v3 acceptance, failed bulk transactions, one-time recovery redemption and quick-death rejection. New UI uses no Ascension selection; old checkpoint fields remain compatible. No human game was restarted. The earlier host audio/startup limitation has not been diagnosed, though the owner's later direct games clearly produced valid 0.20 results.

## Balance benchmark follow-up · 10 September 2026

Added `tests/balance_reference.gd` and `balance_benchmark_test.gd`, plus the acceptance contract in BALANCE_BENCHMARKS_21. Offline 144-case grid uses current ability/mastery/equipment/cost/mitigation functions with identical earned budgets and four mastery focuses. Focused run: 579 integrity checks, zero failures. Generated report remains ignored in `output/balance/current.md`; no profile load/save and no player rewards. Full `scripts/check.ps1` passes all 31 suites, including 188 review and 65 Operation checks, without logged engine errors. Existing WAV exit warnings remain. Behavior probes were not repeated because gameplay is unchanged.

Initial A0 balanced level-24/full-tier-one analytical DPS is 338.5, implying 17.7/29.4/45.0 seconds against Chapter 1/4/8 HP. These are continuous formula quotients, not legal-rotation timings or upper bounds: modules/procs/opening charges/animation interactions are omitted. Chapter-1 minimum melee-tank floor/reference W falls from 1.81 to 0.99 across rounds. These are balance warnings, not reasons to silently retune production HP. No runtime code or encounter schedule changed. Existing audio/startup limitation remains unrelated and unresolved.

9 September 2026. The owner explicitly requested a larger rewrite before judging it, superseding the earlier per-phase human stop gates. This delivers the integrated **Vanguard + progression + Practice** slice, not every item in the roadmap.

## Implemented

### 0.19 deliberate combat · verified 10 September 2026

Owner answered all three final questions: remove chest skill points and the rank-five gate; three paused core selections per level; buy/upgrade run-only modules after every round; EMP suppresses D/F and 1234 while QWER/MG/hammer/walking remain. Implemented details and theoretical damage estimates are in REVIEW_COMBAT_19.md. Existing checkpoints preserve old behavior; new expeditions and primary Practice opt into `review19`. Human-approved boss HP stays unchanged. Mastery restructuring and additional equipment regeneration are deferred, with same-slot tier monotonicity checked.

Verification: complete `scripts/check.ps1` passes all 29 suites with no logged engine errors. Dedicated review fixture passed 138 mechanics/transaction/render assertions in that run, then extended to 142 with actual launch→three UI choices→resume and Equipment Back navigation assertions. Tests cover chest accounting, eligible choices, specialization, credit purchases/failed-save rollback, checkpoint ownership, F charges/D reference upkeep, EMP scope/expiry, W status and boss stun guard, combo consumption, boss HP, equipment tier monotonicity and all four camp tabs. A historical catalog UI fixture now explicitly opts out of review19 when feeding legacy `skill_*` offers; current modal UI is tested separately.

Rendered 1600×900 camp, Build, Mastery, Equipment, upgrade cards, boss/EMP warnings and large-coordinate late rocket impacts were inspected. Camp fits six entries across two rows; upgrade cards use concise descriptions without clipped paragraphs. Normal and reduced effects preserve functional warnings. The disappearing-impact polygon failure did not recur in the final rendered fixture after rejecting degenerate/subpixel shards. No new raster assets. Existing two WAV ObjectDB shutdown warnings remain; no engine-error waiver.

Both refined compatibility probes completed without engine errors; the later adaptive compatibility policies win their legacy route. The legacy expedition comparison remains ranged active round 4 loss, melee active full-route win, summoner active round 5 loss. Current Vanguard A0 crude policies lose early: idle 12.6s, active 19.7s, basics 20.3s. This flags the combined early pressure/mobility/XP changes for human testing, not a player win-rate estimate.

Current A5 artificial-health soak reaches round 21, state `running`, at 7,996.8 simulation seconds (14,730 kills, level 41, peak 180 enemies). It does **not** complete all 22 rounds before the existing budget. p95 step 924µs/max 11,436µs are diagnostic CPU values from concurrently scheduled checks, not a before/after performance comparison or rendered FPS. No engine errors. Full-route completion remains unverified; do not change approved boss HP solely to make this bot finish.

### Frozen-build human review · 10 September 2026 (implementation pending)

Tested clean commit `02ac283fabefaffd404302778212babe3ce8f295`, family `vanguard-18-pressure-audio`. Original baseline 01:37:42 PDT: 20 result lines / 57,034 bytes, last write 01:22:45. First launch inherited a hidden window and logged sandbox-related engine-log/certificate failures. Owner approved recovery restart; same build relaunched visibly at about 01:55 PDT with normal save access and unchanged result baseline. No source/assets, tests or tuning changed during play. Raw logs and launch/session notes remain ignored under `output/playtest-2026-09-10/`.

Matching additions: 01:59:09 A0 round-one loss, 46.3 simulated seconds, level 2, 37 kills; 02:18:03 A0 Stage 1 round 3 loss, 565.83 recorded seconds, 574.93 process wall seconds, level 17, 766 kills, 71 chests. Later earned ranks: Q/W/E/R/orbit 10; D/F/MG/hammer 5; Bulwark/Reserve/Overclock 7. Last cause is generic Hostile projectile, insufficient to identify the enemy. One blink and 13 slams were recorded; counts do not establish reasons for use/nonuse. Fresh/Continue mode remains unconfirmed; no measured per-round damage curve exists.

Later bounded rendered sample: 19,940 frames, median 9.408ms, p95 14.626ms, peak 81 enemies. These timings are not whole-route performance certification. Repeated `Invalid polygon data, triangulation failed` errors trace to `vanguard_motion.gd:11` (`shard`), through `burst` and `workshop_art.gd` impact rendering. This is a real runtime defect not covered by the preceding green regression, not a waived warning. No new tests run yet.

Owner feedback and proposed priorities are recorded in QUALITY_BAR's dated human review. Main-boss HP is approved; progression/chest economy, meaningful enemy/boss counterplay and camp presentation lead the next pass. Owner authorized proceeding after final directional questions about chest/gate policy, module shop cadence and EMP scope. Broader mastery redesign is deferred; preserve existing saves and user music.

### Reusable playtest handoff · 10 September 2026

SESSION_HANDOFF.md is now the stable entry point, linked from AGENTS.md and the documentation index. PLAYTEST_PROTOCOL.md defines frozen-build testing, evidence limits, three focused feedback questions and the update contract for future sessions. Old Phase 0 kickoff wording no longer directs current work.

Completed results now carry schema 2, a current Vanguard build-family label, Practice/automation tags and the twelve earned tool ranks. Existing historical logs are untouched. This is metadata only: no balance change, replay, continuous telemetry or profile migration. `scripts/read_playtest.ps1` summarizes recent local results read-only and filters tagged test runs. Exact commit and fresh/Continue status still require a playtest baseline; the build label is not a commit hash. Results are saved at victory/defeat, not simply on exiting a running game.

Verification: all 28 suites in scripts/check.ps1 pass, including run_diagnostics_test (metadata and simulation isolation). The read-only PowerShell reader also passes a synthetic mixed-history fixture: filtering, explicit test inclusion, last-N selection, rank fields, malformed/missing records and unchanged file hash. Known earlier WAV shutdown warnings remain. Human playtest is the next action; no quality ratings increased.

### Two-minute pressure and audio feedback · 10 September 2026

Owner asks for stronger/more varied enemies, growth every round, shorter survival and clearer injury/energy feedback. Revised rounds now last 120 seconds before the encounter; 12-second collection and all 22 rounds remain. Vanguard adds three native specialist silhouettes/behaviors: committed Breacher rush, bounded Mender healing and Scattergun spread. Practice exposes them without campaign scaling. HP/damage now increase monotonically across route indices; ordinary movement and specialist capacity have caps. Main boss HP stays at the preceding 50× setting; incoming round damage does apply during boss encounters.

Post-hit fraction drives amber feedback below .5 and stronger red feedback below .25, with small persistent critical corner marks. Reduced effects removes the edge glow, never the cue; no added camera motion or central overlay. Original synthesized cues distinguish injury severity, empty energy, unavailable charge, mode switch, deployment, big XP, supply type and chest contents. A duplicate generic supply sound is silenced while preserving its visual event. Injury/energy/threat cues use reserved voices; failure and threat sounds are rate-limited. No new settings, bitmap assets or downloaded sound pack; user music unchanged.

Verification: all 27 regression suites pass; `pressure_feedback_test.gd` has 145 checks covering round monotonicity, exact 120-second boundary, three behaviors, heal/count caps, charge conservation, swept contact, thick-wall collision, detached-camera independence, Practice baseline, full projectile pool, severity thresholds, PCM duration/peaks, voice reservation/mute, normal/reduced visual fixtures. Native new enemies and edge cues inspected at gameplay size. Full regression also retains the 2 pre-existing ObjectDB exit warnings in Vanguard. The A5 artificial-health soak reaches round 20 at the 7,999.4-second budget, still running, with no logged engine error; full-route soak completion is NOT verified. Existing boss HP/damage uptime remain a late-pacing risk. Audio listening and owner difficulty judgments remain pending; technical sample checks are not proof of an enjoyable mix. See [pressure/audio audit](PRESSURE_AUDIO_18.md) for numbers and source rationale.

### Visible chest contents and stage shops · 10 September 2026

Fixed the owner's report that chest opening showed only a box. Vanguard already awarded chest loot automatically; the camp animation had no connection to those outcomes. New simulation-owned reward ledger records actual chest points/credits/items plus round-clear credits and loot-round gear. Combat shows a five-second non-modal receipt; camp retains results with tier-framed gear icons, names and counts. Gear is listed first. Three ordinary reward rows fit without scrolling; larger batches scroll. Lid opening accompanies a short scale/fade reveal; click the receipt to skip, or enable Reduced effects for immediate results. No animation callback awards loot.

Vanguard shops now open after all eight stages (rather than only 2/5/7). Field credits buy permanent equipment; this run-only currency resets on a new run. Stock is generated before checkpointing, survives Continue, and sold slots stay sold. Old checkpoints remain valid; missing historical receipts cannot be reconstructed. Legacy shared-pool shops retain their old schedule. Failed transactions remain visible and retryable.

Verification: `loot_receipt_test.gd` adds fixed-seed checks for exact awards, max-rank fallback, all eight shop boundaries, transaction rollback, duplicate purchase prevention, validation/migration, resume, click-to-skip, reduced effects, and actual-content UI. Render with `-- --render` for ignored review images. The normal three-row receipt and actual-size combat notification were visually inspected; human reward satisfaction and the expanded shop economy remain unvalidated. No player saves are written by these fixtures.

### Rare special drops and capped pickup reach · 10 September 2026

Random special-drop baseline ×0.1, with independent energy/repair rolls and stronger capped Luck contribution. Guaranteed boss credits, XP and chests preserved. Magnet acquisition capped at the turret's normal 320 range, regardless of utility/mastery bonuses; starting 65 unchanged. Disable Vanguard's otherwise dormant global-vacuum fallback. End-round collection preserved. Mastery hover/inspection now shows the stronger chance conversion and magnet cap; no new menu controls.

Vanguard fixture adds 13 assertions for exact baseline/cap formulas, inside/outside reach, no vacuum bypass and fixed-seed real kill distributions (701 total assertions). Full regression and compatibility behavior probes accompany the change. No player collection changes or new assets. Scarcer supplies add energy/healing pressure; human tuning remains pending.

### Equipment identity and stage pressure · 9 September 2026

Forty stable equipment IDs now have distinct names: tier prefixes Salvaged / Tempered / Stormforged / Astral / Sovereign; slot silhouettes Visor / Carapace / Greaves / Striders / Heartcore / Signet / Ironbloom / Mantle. Silver, green, blue, violet and gold inset glows plus 1–5 pips distinguish tiers wherever equipment icons appear. The selection header shows the actual item name; tier text shares its color. Existing artwork, ownership, forging, stats and save IDs are unchanged. No new bitmap assets or skin setting.

Vanguard campaign scaling now covers previously unscaled surge/ranged/add paths. Stage-1 waves and guardians unchanged; later HP and incoming damage grow by central stage functions. Main boss health ×50 and main-boss-encounter incoming damage ×1.25. Exact curves, exceptions and long-fight caution are in BALANCE_16. No further XP reduction. The user's positive Stage-1 report is more relevant to feel than the earlier simple-bot losses.

Full regression passes; Vanguard fixture now 688 assertions, including all eight stages, surge/ranged HP, no double scaling, stage/boss damage, preserved Stage-1 guardian and 40 unique equipment names. Equipment menu captures inspected at real window size; keyboard UI reports 369 labels with no overflow. Both refined legacy probes and expedition compatibility probe retained. Practice tests never award permanent loot. Human Stage-2/boss duration validation remains pending.

### Tower milestones, universal points and drop budget · 9 September 2026

Towers gain side hardware at rank 5 and brass crown/outer collar at 10; Bulwark also gains twin barrel rails. Same native material grammar, no new bitmap pack. Milestone mechanics, mobility numbers and progression tradeoffs are in BALANCE_16's latest addendum. D10 permits abilities/commanded hammer while held, including normal-cast previews; F10 deals one arrival blast; E reach grows at 5/10. Construct range/lifetime rings use the actual values, including live upgrades. Reduced effects retains silhouette/range cues.

Level/chest points are universal. Old queue strings remain valid but no longer restrict spending. Every tool must reach earned rank 5 before purchasing rank 6+; hover explains the gate. Practice presets bypass it. Future XP gain ×1/3, with fractional conservation and no subtraction from existing progress.

Performance: when 80+ XP pickups exist, a 0.5s sweep merges settled, non-pulling pickups in 48-unit spatial buckets. Initial showers remain intact; value and location locality are preserved. Larger bundles have larger scrap silhouettes. Off-screen XP drops skip drawing. Magnet reach/speed are computed once per pickup step, not per object. Final synthetic CPU pickup benchmark: 360→12 objects, 200 ticks ~21.4ms→1.0ms with caching in both cases. This is not an overall FPS measurement, and widely dispersed drops may not merge. Caps and no-reward-loss fallback remain. No loot RNG draws added.

Verification: Vanguard fixture 555 assertions; full regression plus both refined probes and expedition probes. Actual-size normal/reduced rank 1/5/10 tower captures use skill_visual_test `-- --milestones --folder=res://output/tower-milestones`. Existing two-WAV exit leak warning remains. Owner visual/feel approval is pending, with a significant early-pacing warning from the Vanguard bot comparison; no fun score raised.

### Camera correction and compact HUD · 9 September 2026

Supersedes the earlier inverted camera policy below: locked follows only; unlocked edge-pans. Enabling lock cancels inspection/offset and snaps immediately. Existing minimap hold inspection remains a temporary exception; release returns home only when locked. Focused gameplay confines the pointer in either mode; menus/focus loss release it. Minimap mapping accounts for HUD scale.

HUD defaults to 90%, with one 70–100% Options slider. Screen-anchored HUD islands scale independently of menus/world. Controls configures Vanguard 1–4/QWER casts, persisted separately from legacy area-cast preferences. Default Normal: 2/3/4/R. Normal waits for left-click, ignores key release and spends nothing during preview; Esc/right-click cancels. Orbit remains immediate because it has no target. D/F unchanged. Skill previews clear attack-move aim.

All twelve icons use a reversible 32×32 sampling grid, saturation ×0.85, brightness ×0.97. Original masters and 128px imports unchanged. No equipment-art or combat change. 1–4 use rotating active arcs; reduced effects uses stationary arcs. Constructs indicate activity only while deployed; Orbit reflects power availability. Cooldowns remain visible when inactive.

Interface fixture: 212 assertions, including lock/snap, scaled minimap, cast defaults/release/cancel/confirmation, preview charge preservation and construct activity. Full 25-test regression passes. Rendered Options/Controls and normal/reduced HUD inspected; actual-size icon sheets rendered with skill_visual_test. Human readability/feel remains pending; no quality rating raised.

### E wall rebound

Body slam now reflects off a capsule wall's surface normal once per cast. Distance after collision is twice the unused initial dash distance; rebound speed is 1425 versus the initial 950 units/sec. Earlier contact therefore creates more escape distance; late contact creates little. A second wall or arena edge stops motion rather than chaining multipliers. Enemy contact on either leg retains the same damage/push/stun/shield rules and ends the dash. No extra energy/charge or wall-hit damage; a small existing endpoint flash/contact cue marks the bounce. Movement uses four-unit bounded sweeps plus contact bisection, including rounded ends. Rebound is transient and reset by new casts/Practice clear. Directional shoulder/trails follow the reflected direction. Deterministic tests cover distance at 120/60/30/10/2 Hz, angled impact, late impact, second walls, enemy impact and arena limits. Human escape feel remains pending.

Focused Vanguard verification: 491 assertions. Full regression and normal/reduced reflected-motion captures accompany this change. Both refined probes complete without engine errors. Fixed-seed Vanguard A0: idle 17.4s; naive active 115.1s / round 1; basic-attack policy 2826.7s / round 15. The naive policy does much worse than the prior snapshot: a compulsory rebound changes positioning and exact impact timing; this is a reason to playtest escape steering, not a human win-rate claim. Enemy damage/cooldowns were not retuned. Existing intermittent two-WAV exit warning remains separate from engine-error failures.

### Complete Vanguard painted icon family

Owner approved QWER's direction; eight new generated icons cover D/F, slots 1–4, hammer and MG. All twelve now map through VanguardHud for HUD, Practice and Build, with opaque 128×128 mipmapped imports and retained native missing-asset fallbacks. QWER unchanged; no equipment, world-animation, damage or ability changes. A rocket-shaped D draft was rejected and replaced by a propulsion boot. Provenance and exact prompts: `assets/vanguard_icons/REMAINING_PROVENANCE.md`. Interface/skill visual fixtures now validate the full family; `--icons` captures all twelve at 54/40/32px in normal/reduced modes. Owner approval of the eight new icons remains pending; no quality score raised.

Verification: 25-script regression; full-family interface fixture 159 checks and skill visual fixture 2984 checks. Actual-size HUD and icon sheets reviewed in normal/reduced modes. Dark label outlines preserve key/rank/state readability against brighter paintings. Both refined behavior probes and legacy class compatibility complete without engine errors. No combat tuning; no new human gameplay or audio-quality claim.

### Interface, camera and icon follow-up

- Four original generated Vanguard QWER paintings are official, with 128×128 mipmapped imports. Other skills/equipment retain existing Painted artwork. No skin selector; masters/provenance retained. No world animation or damage changes.
- Blank custom tooltips return no panel. Sound is a live master-volume slider, zero mutes both music/effects and 100% restores the existing full mix. Camera-speed percent and confined-gameplay mouse multiplier sliders join four compact toggles. Persisted settings migrate without replacing equipment or checkpoints.
- Per the owner's explicit wording, lock **on** enables visible cursor confinement and edge-panning as a follow offset. Lock off releases the pointer and disables edge-panning. Space clears the offset; L retains its rebind support. Menus/focus loss release confinement. Left-click minimap drag temporarily detaches; release snaps back when locked or retains the location when unlocked. Spawns remain player-centered. Godot's [Input mouse modes](https://docs.godotengine.org/en/stable/classes/class_input.html) and [Viewport warp_mouse](https://docs.godotengine.org/en/stable/classes/class_viewport.html#class-viewport-method-warp-mouse) underpin the Windows pointer handling. Desktop mouse settings are never changed.
- Practice has Build/Player/Enemies only; shared rank 1/5/10 includes hammer and MG. Damage numbers moved to Player. No per-skill rank controls, Clear button, Session metrics/speed menu or Legacy selector in the primary dock. Existing legacy fixtures remain. Reset remains. B arms one dummy; C clears enemies/hazards/meters without pausing or removing friendly constructs. Explicit system-key rebinds take priority over shortcuts. No permanent loot or checkpoint writes.
- Actual-size visual inspection found zero-height slider tracks in the first draft; fixed before handoff. Q/W silhouettes are the clearest; E/R are richer and need owner preference review. No numerical quality score change. Physical pointer feel across DPI/multiple monitors and audio listening remain human checks, not established by assertions.
- Verification: all 25 regression scripts pass; interface fixture covers volume endpoints, shared ranks, removed controls, B/C scope, minimap lock/release and mouse-binding priority, edge-pan gates, cursor scaling, source alpha and 128px imports. Rendered normal/reduced HUD and settings inspected. Both refined probes, legacy class compatibility and fixed-seed Vanguard probes completed without engine errors. Vanguard A0 snapshots remain idle 17.4s, active round 16 / 3026.4s, basics round 15 / 2829.4s; these are diagnostics, not human win rates. Existing intermittent audio-resource exit warnings remain tracked separately.

### Always-fast orbit follow-up

Vanguard slot 1 now rotates at 10.2 radians/second in both near and far modes, retaining continuous phase when toggled. The toggle still changes radius. Energy upkeep, blade count, per-hit damage and each mode's existing hit lockout are unchanged; faster far rotation can change realized contact frequency. This supersedes all earlier far/slow descriptions. Focused regression asserts equal angular advance in both modes.

### Vanguard-only motion review · 9 September 2026

Original pose-based VFX helper: eight-pose directional/material bursts, six-pose blink brackets, narrow W dart versus wide caged R drum, faster hammer stroke/recovery, rounded D echoes aligned with travel, 240ms construct leg deployment, Reserve transfer motes and in-range Overclock brackets. MG receives a quiet 65ms muzzle cue. Existing Q/orbit silhouettes are retained. Fixed overlapping legacy W/R impact rings and the legacy interception of Q's custom impact; audio events remain intact.

No damage, cast delay, collision, cooldown or save-format changes. Cosmetic source metadata distinguishes support from damaging impact presentation. `VANGUARD_ANIMATION_REVIEW_18.md` documents every animation's maturity and remaining work, resource/licensing research and reproducible native-rendered review video. No external/generated bitmap dependency was added. Global quality ratings are unchanged; the result is a style candidate for owner review, not a finished directional character rig.

Verification: all 24 regression scripts passed; Vanguard 460 checks, standard skill-visual 2,972 checks, native Vanguard reel 2,936 checks / 1,440 frames. Both refined probes and legacy-class probe ran without engine errors. A0 fixed-seed outcomes and the 22-round A5 artificial-health soak match the previous MG build's simulation outcomes. Remaining warning: the broad suite and a focused verbose rerun reported two retained audio objects at process exit (`AudioStreamWAV` and `AudioStreamPlaybackWAV`). Existing audio teardown already stops/releases streams; the intermittent engine/test-exit lifetime issue is not fixed or proven to grow during gameplay. Record it for the audio/release audit rather than silently treating this as a completely warning-free build. Native review reel itself exits cleanly.

### HUD grouping · 9 September 2026

Upgrade affordances are now full-icon-width, 16px-high bars above each eligible skill, with unchanged spend semantics. Icons are 48px, with QWER only slightly larger at 54px. The 1234 group sits on the left; QWER anchors the center with a brass baseline; D/F and hammer/MG sit in separate quiet right-hand groups. All icons bottom-align. Removed the 5/6 HUD buttons and redundant MODULES/MOBILITY captions. This is a presentation-only change: existing consumable keyboard shortcuts and saves remain intact.

Rendered gameplay-size review shows clear spacing and no covered key labels. Vanguard fixture now checks actual post-layout icon/button bounds, all pairwise icon overlaps, absence of consumable buttons and a real upgrade-button signal targeting the hammer. 460 focused assertions pass. No balance, progression or input changes; previous simulation probe evidence remains applicable.

### Machine-gun milestones · 9 September 2026

Supersedes earlier unconditional D/E firing and MG damage/cadence. Existing rank 1–10 upgrade UI/save track is retained. Rank 1 starts at 1.2 damage / 0.24s; rank 5 at 1.8 / 0.20s; rank 10 at 2.88 / 0.15s. Damage and speed improve at every rank. Before rank 5, held Ghost drive and body/ordinary dashes suppress new shots, without toggling the gun off or building catch-up volleys. Instant F has no sustained dash interval. S and walking remain independent.

Rank 10: every fifth successfully emitted shot deals 2× damage, travels at 950, reaches 450 and penetrates three times (four targets). Normal rounds reach 265 and do not gain legacy power-track pierce. Existing projectiles are not destroyed when a dash starts. Counters advance only on successful fire; toggle/pause/full pools do not consume the fifth shot. Counters are combat state, reset with a fresh kit/resume, not persistent equipment progress.

Bulwark's gun uses MG power/rate/milestones, with its own cycle, 320 ordinary / 520 special range and 3.4 baseline damage per 0.8s. It continues firing during player movement and while the player MG is toggled off. Bulwark ranks still improve HP/pulse; no double multiplication of both weapon damage curves. The tier-5 follower inherits ordinary MG damage/cadence only, unchanged in scope; it does not borrow the player's fifth-shot range/counter. Special tracers have a longer brass/cream core; the turret barrel points toward its actual last shot and recoils on its actual cadence. Normal/reduced captures and simulation-independent visual fixtures cover the new shot.

Verification: all 24 regression scripts passed, 266 Vanguard checks, 2,972 headless skill-visual checks and 1,456 rendered showcase checks. Both refined legacy probes and all legacy classes ran without logged engine errors. Current fixed-seed A0 policies lost at 17.4s idle, 3,026.4s active (round 16), and 2,829.4s basics (round 15). Final A5 artificial-health soak completed round 22 at 4,354.3 simulated seconds; peak 68 enemies, p95 simulation step 1,379µs on this host. Bot results are sensitive to combat/loot sequences and are not human balance evidence. No quality ratings changed; inspect early MG weakness and rank-ten density payoff in Practice next.

### Rank milestones and native animation · 9 September 2026

- Commanded hammer is a separate run-only rank 1–10 upgrade (HUD LMB tile; Practice Hammer selector). Old checkpoints default to rank 1; malformed ranks are rejected. Rank 5: reach 125→156.25 and sweep 90°→120°. Rank 10: reach 187.5, sweep 140°, no swing root; attack-move can advance during attacks. Ordinary target approach still stops at its destination. S remains an explicit stop.
- Shared Vanguard damage curve has small steps and spikes at 5/10. Q/W/E/R retain their existing recharge and area gains. R's rank-10 primary/echo split is 80/20 of the total damage budget. MG gains modest milestone cadence; orbit compensates partially for extra blades. Bulwark damage/HP and Reserve throughput scale too; Overclock remains double recharge, not an escalating recharge multiplier.
- Hammer anticipation, eased sweep and charged head; graded rocket exhaust, MG tracers, orbit trails, E armor accents, W/R core bands and impact rings, D exhaust chevrons, blink/deployment rings, construct rotating status indicators and gun recoil. All are native code art in the existing palette, not a new bitmap pack. No new camera shake. Area boundaries read simulation geometry.
- Focused tests cover hammer progression, migration, collision arc, root removal, moving attack orders and R's echo budget. Visual fixture adds hammer at 1/5/10, normal/reduced; ignored render captures inspected at gameplay size. Human satisfaction is unverified; quality ratings remain unchanged.
- Final verification: all 24 `scripts/check.ps1` scripts passed; Vanguard 182 checks; skill visual 2,912 headless checks and 1,396 rendered showcase checks. Both refined behavior probes and legacy-class probe ran without engine errors. Fixed-seed Vanguard policies: idle lost at 16.7s; naive active at 56.4s; adaptive basics reached round 18 and lost at 3,422.6s. A5 artificial-health soak completed all 22 rounds in 4,345.5 simulated seconds, peak 70 enemies, p95 step 1,073µs on this host. These are bot/stability measurements, not player win rates. Existing test-only PNG resource-load warnings remain; no logged engine errors.

### Alt-Tab pause follow-up

Esc now resumes the paused screen directly after focus loss, instead of opening Settings on top of it. Returning focus alone still leaves combat paused. Esc during normal play continues to open Settings; closing Settings returns to its prior screen. The existing Settings-key remapping still applies. Five focused assertions cover this sequence.

### Cursor presentation pass

- Original vector cursor family: menu pointer, combat precision reticle, and brass aim/placement diamond. Menu pointer takes priority over interactive UI. Existing text/resize cursors are not replaced.
- Native cursor images are cached and changed only on state transitions. No simulated follower sprite, trail, animated wobble, input remapping or camera dependency. Cursor overrides are cleared when the host scene exits; headless tests skip platform cursor installation.
- Dedicated test: 15 state/size/transparency/hotspot checks, plus three native-install/transition checks in rendered mode. A 40/80px light/dark contrast sheet is ignored under output/cursors. No gameplay balance or player-save changes.
- All 24 regression scripts passed without logged engine errors; the final rendered cursor test passed all 18 checks after native-size inspection. Human preference and hardware/DPI coverage beyond this Windows machine remain unverified.

### Third playtest: permeable shot cover

- Current expedition/Practice rules: walls block bodies, not friendly/hostile projectiles, returning blades, ranged attack acquisition or enemy laser rays. Legacy pre-rules17 fixtures retain their old cover behavior. Ground-area casts already cross walls; E movement still collides, and construct placement still requires free space.
- Vanguard E now stores two charges from rank one, refills sequentially at its existing recharge rate, restores two at cleared-round resume, and shows charge pips/hover details.
- Character hull/energy bars move below the feet, retaining screen-size scaling and translucent backgrounds. Top HUD resources are unchanged.
- Vanguard checks expand to 129, including through-wall damage in both directions, full-length laser geometry, retained body collision, E sequential refill and Practice instant recharge restoring both charges. Actual-size bars were visually inspected. One first rendered shutdown reported a resource cleanup error; headless and verbose rendered repeats were clean. This is not evidence of a persistent gameplay leak or proof the intermittent shutdown issue cannot recur.
- Final regression: all 23 scripts passed with no logged engine errors. Both refined legacy probes completed. Current fixed-seed Vanguard A0 lost at 16.7s idle, 56.4s active and round 7 / 1321.6s basics; this highlights changed pressure, not human win rates. Artificial-health A5 completed all 22 rounds in 4411.3 simulated seconds. These probe snapshots precede the final shared E capacity metadata fix (relevant to instant-recharge tests, now explicitly covered). No subjective quality scores changed.

### Second playtest: controls, wall clicks and Vanguard impact

- Camera lock is a fifth rebindable control (default L). It accepts unused supported keyboard keys or any of the nine Godot mouse button/wheel inputs. A mouse camera binding consumes that button's gameplay action. Old four-key settings merge the new default without discarding their bindings; no automatic save rewrite.
- Backtick (`) toggles the permanent gun on/off, independently of S/D. MG shows OFF; the key is protected from other bindings. Starts on for a fresh kit; no new permanent preference.
- Movement clicks inside capsule obstacles project to the nearest surface with player-body clearance. Centerline ties choose the player's side. Routing then approaches the projected destination, instead of chasing a point inside collision. This is not a general concave navigation rewrite.
- Q retains two-charge storage, now explicitly shown by charge pips; rank-one recharge is 4s before equipment/mastery. W now has two charges from rank one instead of waiting for rank five. Sequential recharge remains; rank-five W still gains its normal area milestone.
- Close orbit angular speed is 10.2 rad/s (previously 3.4), versus unchanged far 1.6. Phase stays continuous when toggling. Close per-blade hit cooldown falls from 0.28 to 0.14s; actual damage depends on positioning and contact, not an assumed threefold DPS gain.
- E leads with a steel/brass shoulder rim, then a distinct expanding collision burst. W/R canisters have fins, cores and accelerating descent; R adds a larger reactor and staged flash, shock rings, blast petals and shards. Reduced effects removes secondary detail while keeping attack information. No new bitmap assets, damage radius inflation or camera shake.
- Practice Enemies defaults to Target dummy / 1; the formation selector is removed and Cluster is always used in the primary dock. Older formation helpers remain regression fixtures.
- Targeted verification: Vanguard suite now has 121 checks including charge timing, camera mouse/key input, gun toggle, placement defaults, attack-move projection and reaching three projected wall targets without oscillation. Final full-regression and visual checks are recorded in the handoff; human feel remains unscored.
- Second-playtest verification: all 23 regression scripts pass with no logged engine errors; final skill-visual check passes 2852 assertions. Rendered E travel/contact and R detonation were inspected at actual combat scale in normal/reduced modes. Both refined legacy probes completed. With the current Vanguard fixed seed, idle still loses at 16.7s, active reaches round 14 before losing at 2653.5s, and basics reaches round 9 before losing at 1723.7s. These scripted policies include other abilities and do not isolate orbit DPS or prove human balance. No quality ratings were raised.
- The second-playtest artificial-health A5 soak completed all 22 rounds in 4428.9 simulated seconds (peak 69 enemies). Probe snapshots precede the final A-click destination projection fix; right-click projection was already included. This is route reliability, not a human difficulty assessment.

### Post-playtest Practice / HUD pass

- Practice-only floating damage numbers, enabled by default and toggled in Session. Short same-target tick coalescing and a 96-label cap limit clutter; fractional damage is retained.
- Immortal target dummies show per-target total damage and first-to-latest-hit DPS. Three simulation seconds without a hit clears the burst; paused configuration consumes no time. The last DPS holds during that grace, and a single instantaneous hit shows a dash rather than fabricated DPS. All damage sources count, including the independent gun. Dummies remain fixed and grant no kills/loot.
- Manual measurement/reset clears scratch meters. No save format or persistent preferences changed.
- Restrained HUD: compact resource/timer/mastery islands replace the full-width top slab; lower action-dock backing, smaller minimap, retained icon sizes/horizontal key positions and corrected toggle-Tab tooltip. No gameplay tuning or new art pack.

### Integrated kit

- New expeditions: fixed Vanguard. Permanent autonomous gun has no slot and ignores S/D. Q/D/F start learned; the other tools begin locked. Old checkpoints continue their original shared-pool rules; no forced conversion or lost skills.
- Left-click hammer: 0.20s commitment, 90° arc, 125 base reach, 1.05s base cadence. The outer head (64+ base distance) deals 38 unscaled damage, push and 0.25s ordinary-enemy stun; handle deals 9. Right-click/A can approach and repeat it. Attack mastery scales damage/speed/range, with matching visual reach. This replaces ranged commanded basics for Vanguard only.
- Q retains the distinct straight exploding rocket, with 80ms anticipation. W reaches 650 units and has a delayed 100-radius strike, 40-radius double-damage center, 38/76 unscaled damage; rank 5 stores two charges, rank 10 stuns ordinary enemies briefly.
- E is a short body slam: 210 nominal reach, collision stops at the first enemy/terrain, 95-radius 32-base-damage impact and push. Base protection suppresses touch only. Rank 5 adds stun. Rank 10 grants full protection during the dash and one second after enemy impact; a miss does not award that extra second.
- R is a 170-base-radius reactor impact after anticipation + 0.75s descent, 135 unscaled damage. Rank 5 shields the player when inside; rank 10 repeats the impact after 0.4s and stuns ordinary enemies. Click confirmation remains the default.
- D is held speed, not a timed invulnerability charge. Starts at +65% speed / 10 energy per second; ranks improve speed and efficiency. Release ends it. Blocks manual attacks/casts/redeployments while the gun, orbit and existing constructs continue. No initial activation cost or cooldown. Start sound and optional afterimages.
- F is instantaneous. An unreleased Q/W/R within its 80ms anticipation uses the new origin, preserving the original world aim point. Released effects do not move retroactively. Invalid terrain endpoints resolve to the near/far side according to midpoint penetration, using body clearance and a bounded search; blocked arena exits fall back safely. Endpoint poofs and a separate sound replace travel effects.
- 1: permanent powered orbit blades, close/fast versus far/slow. Ranks add damage, blades and milestone reach. Reactivation after energy shutdown is explicit via 1.
- 2: one replaceable Bulwark, durable and destructible, weak gun/local pulse, attracts at most four ordinary enemies inside 280 range. Bosses and ranged specialists retain player targeting.
- 3: one Reserve totem banks repair while away, then spends it on hull → energy → a capped-rate overflow shock. Redeployment clears stored value. Untargetable; expires after 35s.
- 4: one 5s Overclock well. Inside: free active energy and double recharge. Cooldown starts after expiry; it does not accelerate its own recharge. Untargetable.
- Level/chest opportunities queue upgrade/learn alternation. The front of the queue determines eligible slots; empty categories fall back to the other category. Maxed kits convert unspendable opportunities to field credits. Click small + controls or Ctrl+key, without pausing/casting. No replacement, cross-kit fitting or binding swaps. XP pacing is unchanged. Utility/mastery remain separate.
- Stable module/core/mobility HUD groups, short state labels and hover details. Consumables remain 5/6. T is unused in Vanguard. The old menu robot positioning fix is preserved.
- Practice: collapsible left dock, rank presets and per-key rank overrides, god/free-energy/recharge/frozen-enemy controls, time scale, clear/reset, simple total-DPS measurement, and paused world placements in Point/Line/Ring/Cluster formations. Overlapping/blocked batches reject atomically. Shift repeats, right-click/Esc cancel. A separate Legacy laboratory starts a clean shared-pool scratch run.
- Practice uses five substantial capsule obstacles in a compact test area within the existing arena. Collision, routing, projectiles, laser cover and spawn previews respect thickness. This is a greybox, not a finished map. Campaign terrain remains the prior rail layout.
- Practice kills grant no pickups, credits, chests or equipment. Campaign equipment/settings/music are preserved. New reward queues are validated within existing checkpoint payloads; no external save service or new permanent economy.

## Verification

Post-playtest follow-up: all 23 scripts in check.ps1 pass with no logged engine errors. Practice meter adds 12 deterministic checks; Vanguard retains 96 checks. The older basic-attack fixture now asserts recorded damage instead of dummy health loss, matching immortality. Actual-size 1600×900 HUD/dummy captures were inspected in normal and reduced effects. Existing subjective ratings remain unchanged. Both refined legacy behavior probes completed; current Vanguard A0 policies lost (idle 16.7s, active round 6 at 1055.7s, basics 41.4s). A5 artificial-health soak completed all 22 rounds in 4460.1 simulated seconds, peak 82 enemies. These are crude fixed-seed diagnostics, not human balance evidence. No campaign balance changes were made in this follow-up.

The pre-change 21-script regression passed, followed by the expanded 22-script regression with zero logged engine errors. New dedicated checks cover starter ownership, reward alternation, malformed saves, round-trip restoration, S/D gun independence, cast-origin blink, near/far thick terrain, source-specific shielding, hammer/slam damage, totem expiry and aura bounds, every active at ranks 1/5/10, collection transition, no Practice loot, placement and input isolation. Vanguard test: 96 checks, no failures. Expanded skill_visual_test: 2852 checks, no failures; covers Vanguard in normal/reduced modes and asserts rendering never mutates simulation. Both required --refined legacy behavior probes completed: passive/naive policies lose; the later adaptive policies finish the legacy route. These are compatibility comparisons, not Vanguard balance results.

Rendered 1600×900 fixtures were inspected for HUD/Practice layout and native combat effects. First-pass upgrade buttons overlapped group labels; they were reduced to small corner affordances. Captures remain ignored under output/vanguard. Existing icon masters are reused, not newly generated art.

Development snapshot, fixed seed 7127, A0, no artificial health: idle lost at 16.7s; moving/casting reached round 9 and lost at 1665.9s; the basic-attack policy lost at 41.4s. Recorded before the final W-range/hammer-range-stat correction. These are crude policies, **not human win rates** or evidence the hammer is balanced. The active policy makes unrealistic repeated decisions and does not evaluate player satisfaction.

The same development snapshot's A5 artificial-health soak finished all 22 rounds: 4448.6 simulated seconds, 21,158 kills, level 143, peak 84 enemies, p95 simulation step 1469µs, max 9432µs on this machine. This tests route reliability, not balance, rendering performance or fun. The inherited result labels still say the legacy class; the --vanguard flag explicitly installs Vanguard and removes class bonuses.

## Design defaults needing your verdict

1. D trades casting for speed and energy, without invulnerability. Does that feel like a useful choice or an unnecessary restriction?
2. Hammer remains deliberately committed. Its exposed basic-attack bot did poorly; do not assume a numbers pass solved melee risk.
3. R returns to a reactor drop for this kit. The old laser remains available in Legacy laboratory, not removed from the project.
4. Deferred choices are FIFO, capped at 256. MG upgrades use its own + rather than assigning another combat key. Does the flow stay noticeable but unobtrusive?
5. Totems create positioning decisions; healing storage and energy windows need real boss testing, not only isolated DPS.

## Remaining work / honest limits

- Marshal, Racer, visible Robot AI, offline rewards, new equipment economies and the parked combo kit are **not implemented**. Their specs remain available for follow-up.
- No campaign map rewrite, authored wave-grammar pass, original boss bodies, new bitmap pack or top-down robot rig.
- No general navmesh solver. Thick cover is capsule geometry; arbitrary touching/concave obstacle collections still need a broader terrain pass.
- No equipment editor, named Practice presets, detailed per-source results panel or replay recording. Current metric is total damage divided by elapsed simulation time; it is not a rolling hit-rate estimator.
- Some reused icons have close silhouettes; module 4's converter icon is a reuse, not bespoke art. Native effects need a human weight/readability verdict and audio listening.
- Fullscreen/export, low-end hardware and clean-machine certification remain outside this pass. The long 22-round route remains an open pacing concern.

## Suggested test

Start a **new expedition** to get Vanguard; Continue preserves an old build. Or choose Practice, rank 5, place ten bumpers in a cluster and one ranged threat. Compare hammer/Q/W/E; hold D while moving, then release into F/Q. Try Reserve and Overclock well against the boss. Judge this as one integrated prototype; report the three things that feel best and the three that need pruning.
# 10 September · 0.19.1 progression follow-up evidence

Starting checkout: clean `fcc5164`, following implementation `97d9747`. No active human test. Latest owner feedback and source-backed campaign proposal: PROGRESSION_RESEARCH_20. The single Chapter/Operation directional question remains pending; current route stays 22 rounds.

- Numerical cards use a separate preview kit and leave live ranks, RNG and cooldowns unchanged. Tests compare W preview against the actual released effect, check F charge changes and equipment-rank caps. Mechanics stay in hover details.
- New `unified_mastery` runs have one root, Offense/Defense/Utility and 13 nodes (29 total spends), with one point/level. Save validation checks root dependencies, allowed nodes and point budgets. Resume preserves mode and stats; old checkpoints retain the earlier tree. Tier-three-and-up helmets/chests now provide energy/health regen without changing inventory ownership or item IDs.
- Most specialist roles appear by round two; EMP enters at Stage 2. Corrected W reference floor to include 1.2 kit multiplier. Actual no-gear center W at ranks 1/5/9/10 is 91.2/145.92/207.936/300.96. `tests/progression_budget.gd` gives explicitly limited theoretical damage/energy references, not human damage estimates.
- Reviewed bosses show five-minute overload countdown. After it expires, incoming damage grows to 10x over 90 seconds, gapped rotating rings accelerate, and boss recovery shortens. Physical threats remain bounded by projectile caps; no direct timer loss. This is a tuning starting point, not a human fairness claim.
- New result records contain the most recent 96 completed 15-second intervals with earned ranks, level, XP, mastery spend, field credits, boss HP and all-target credited damage rates. Intervals rebase after Continue/round changes; incomplete intervals are omitted. Reader exposes samples. No inferred historical timeline, hit rate or boss-only DPS.
- Final `scripts/check.ps1`: all 29 suites pass; focused review suite **188 checks, zero failures**. Existing two WAV ObjectDB exit warnings remain. `git diff --check` clean. 1600x900 upgrade/mastery/camp/combat renders inspected; explicit `--audio-driver Dummy` avoids unavailable host WASAPI in the rendering fixture. Initial default-audio render reproduced WASAPI initialization errors. Silent rendering is not audio verification, and no new owner game was launched.
- Both refined behavior comparisons complete without engine errors. Current A0 Vanguard idle/active/basics lose in round one at 12.6/19.4/20.3 seconds with 8/17/24 kills; crude policy evidence, not human win rates.
- Updated A5 artificial-health soak: **round 21, state running at 7,996.8 simulated seconds**, level 41, 14,869 kills, peak 180 enemies; p95 CPU step 891 microseconds, max 10,961. Process exits 1 for incomplete route. Tests ran concurrently, so timings are not a controlled before/after performance comparison. No full-route success claimed; no permanent rewards written.

Raw outputs remain ignored under `output/playtest-2026-09-10/followup-*`; images under `output/review19`. Next action is the pending campaign decision and then human pacing/boss/readability review; preserve earlier human ratings and approved boss HP.
# 10 September · 0.20 Chapter Operations milestone

Follow-on from verified `e7abcfe`. After the owner said "status? stuck? keep going", continued with the explicitly stated working Chapter/Operation direction. New specification: OPERATIONS_20. No selected questionnaire response is claimed. Old checkpoints/collection/settings/music are preserved; no player currencies were spent by automation.

- Eight replayable Chapters, three-round Operations, 90/105/120-second survival. Short-run builds reset; old 22-round checkpoints retain their route. Core cap level 26 (75 picks), 20 XP/level; survival foundation plus density-normalized pickups. New boss budget and 180-second overload, with old-route HP/deadline intact.
- Field credits purchase modules only; Operation chest/coin drops do not create persistent currency/items. Cleared rounds bank Salvage; first-clear bonus/unlock is idempotent. Home equipment crates use 150 Salvage with tier odds 80/17/2.8/0.2/0%. v3 profile accepts older files without optional new fields; failed transactions restore RNG, inventory, balances and Chapter state.
- **Final full regression: 30 suites pass**, including **188 review checks and 65 Operation checks**. Operation checks include all eight Chapter boss configurations, migration/resume, reset boundaries, first-clear repeat prevention, real menu transactions, and a 5,000-crate deterministic distribution fixture. Existing two WAV ObjectDB exit warnings remain. No engine errors in the final headless suite.
- Native 1600x900 Chapter selection, field shop, Equipment and crate-result screens inspected with explicit Dummy audio. An empty-receipt Tween error discovered in the new screen fixture was fixed by avoiding empty animations; final render run has no errors. The known host WASAPI failure remains outside silent-render verification.
- Fixed seed 7127 / loot seed 8028, A5, artificial-health Chapter 1: **won**, 480.9 simulated seconds, 712 kills. Encounter arrivals at 90.0/226.3/386.9 seconds and levels **5/14/23**; finishes level 25. Final encounter to completion is ~94 seconds including collection. Peak 44 enemies; p95 CPU step 863 microseconds.
- Same seed/A5/artificial-health Chapter 8: **won**, 773.5 simulated seconds, 3,299 kills. Arrivals at 90.0/288.5/523.9 seconds and levels **5/14/24**; finishes level 26. Final encounter to completion is ~249.6 seconds including collection, so this undergeared policy survives beyond overload only because it has artificial health. This is **not a human fairness pass**. Peak 180 enemies; p95 CPU step 5,058 microseconds. Concurrent checks make these diagnostic timings unsuitable for before/after FPS claims.
- Ordinary-health Chapter 1 A0 policies still lose early: idle 19.8s/14 kills, active 28.3s/31 kills, basics 26.3s/36 kills, all before the first encounter. Do not treat these bots as human win-rate measurements. Both refined compatibility probes and legacy-class comparisons were completed during the preceding verified follow-up; current modifications are additionally covered by full regression.

Raw outputs: ignored `output/playtest-2026-09-10/operation-*`; captures: `output/operations20`. New human review should target early unlock/card pacing, actual boss counterplay/deadline and the split economy. The short-route test successes replace the need to claim a completed legacy soak; the historical 22-round soak remains incomplete evidence below.


## 12 September 2026 - demo27 longer three-level route

Owner clarified three selectable Levels, each with three five-minute stages; existing Level 3 already feels very hard. Implemented DEMO_PACING_27, gradual Level 1 roster, slower pickup/shop/survival progression, capped new selection, pacing checkpoint flag and triple-base-HP Hatchery. Existing current Hatchery child behavior was already fast runners; now explicitly covered by regression. Enemy damage/HP scaling and boss budget unchanged.

Verification: scripts/check.ps1 passed all 37 suites, including 32 focused demo checks and 47 level-progression checks. Initial test mistakes (nonexistent add_xp helper and terrain-blocked child fixture) were corrected; the old eight-level UI expectation was updated to the new three-level selection. Final run has no engine errors. Rendered level-progression test also passed 47 checks; output/levels22/Levels.png inspected at 1600x900, three choices and description fit without clipping. No bitmap assets changed.

Current artificial-health expedition probes --vanguard --soak completed Level 1 at 1088.4 seconds and --chapter=3 at 1172.4 seconds. Boss-arrival player levels were 18/19, final level 19 for both. Peak populations 28/112; CPU p95 step 835/2259 microseconds in this run. Concurrent test load makes timing descriptive only, not a frame-rate benchmark. The policy label retains historical class=summoner although --vanguard sets the current kit. Normal-health Level 3 idle/active/basics policies lost at 10.7/30.3/22.6 seconds; this does not validate human balance and supports keeping the owner's hard-Level-3 concern open. Both historical mobabot09 and mobabot10 --refined probes completed, preserving passive/moving/adaptive comparisons. Logs are ignored output/demo27-* files; no player currency or checkpoints awarded.

No running human game restarted, no release rebuilt/uploaded. Existing saves retain old pacing; start a new level to review demo27. Handoff and QUALITY_BAR updated without changing human quality ratings.


## 12 September 2026 - stronger ranged pressure and swarm bodies

Owner requested earlier/farther lance threats, aerial bombers firing more over time, and enemies occupying individual space. DEMO_PACING_27 follow-up records the exact values. Current arsenal combat uses spatially bucketed soft separation, committed attack anchors, wall-safe correction, sequential retargeted bomb drops and longer lance tell/hit geometry. Demo roster introduces lancer at 120 seconds.

scripts/check.ps1 passed all 38 suites with no engine errors, including 11 swarm-pressure checks: range/intro, capped barrage, fresh target per bomb, individual warning time, recovery, coincident-body spreading, fixed attack tell, Practice freeze, wall containment, and maximum-population bounds. The rendered test also passed; output/swarm-spacing.png inspected at 1600x900 with 45 distinct bodies. No generated bitmap assets or player saves.

Initial 180-body spacing averaged 10100 microseconds; smaller 64-unit buckets, cached weights and a single solver pass reduced it to 2313-2341 microseconds in subsequent isolated runs. This measures only spacing on a synthetic packed formation, not total game/render time. Current Level 3 --vanguard --soak completed in 1178.1 seconds; final boss arrived at player level 19, peak enemies 161, p95 simulation 5016 microseconds and max 19730. The artificial-health probe is reliability evidence, not human balance or GPU FPS. The pre-change demo probe's peak was lower, so do not treat the whole-run timing difference as a controlled separation-only benchmark. Human crowded-fight feel and later-stage bomber pressure remain to review.

Historical mobabot09 and mobabot10 --refined comparisons were rerun separately. Logs and screenshot remain ignored under output/swarm-*. No player game restarted or release uploaded. Handoff and dated feedback updated; no fun/quality score raised.


## 12 September 2026 - encounter pressure 28

Owner requested tanks near swarm speed, removal of Uplink/EMP, more miniboss counterplay and more aggressive final boss offense. ENCOUNTER_PRESSURE_28 records implementation and boundaries. Tank chase was already 130 before the shared multiplier; now explicitly 90% swarm with shorter melee recovery. Removed enemies remain only in historical code/direct compatibility fixtures, not current campaign or Practice choices.

scripts/check.ps1 passed all 39 suites without engine errors. New miniboss test passed 14 checks covering retired demo roster, once-per-stage non-objective encounters, speed/turn cap, recovery, guard and exposed damage factors, alternating attacks, center-safe shockwave, in-run chest, tank pace, four final-boss fans and 0.45s charge warning. Earlier demo test count falls from 32 to 30 because two specialist introduction entries were removed. Both required historical --refined behavior probes completed without engine errors.

Normal/reduced native-art captures were inspected at actual 1600x900 output scale. Initial tiny health fill was hidden by a default border; fixed and rerendered. Final health bars, tracked bodies, shield ring, ram direction and inner/outer shockwave boundaries are visible. Output/minibosses.png and minibosses-reduced.png are ignored review captures, not generated game assets. No external asset provenance required.

Current Level 3 --vanguard --chapter=3 --soak completed in 1210.4 seconds, boss arrival player level 18, final player level 19. Peak enemies 160; p95 simulation step 6522 microseconds, max 28546 under concurrent verification load. Artificial health validates route completion, not dodge fairness, human difficulty or rendered FPS. New encounters should be human-tested before further difficulty increases. The roaming minis do not replace or automatically complete existing stage-end objectives. Test rewards remain in-memory; no player saves/music changed and no game restarted.

Stable handoff/dated feedback updated. Local source milestone; GitHub release remains unchanged.


## 12 September 2026 - modest XP and Level 1 easing

Demo XP +10%; Level 1 health/incoming damage -10%, including its bosses. Health modifier runs once after role floors; main-boss spawn budget handles its separate scaling path. Practice and Levels 2/3 enemy stats unchanged. Credits and chest probability remain separate from the XP multiplier.

scripts/check.ps1 passed all 40 suites. A subsequent review caught that saved survival-XP validation still capped counters at 40/60/80. Replaced both pacing and validation calculations with OperationRules.survival_budget, permitting 44/66/88 only for demo27. After this final adjustment, focused demo_easing_test passed 43 checks (scope, once-only scaling, pickup XP, idempotent survival award, current checkpoint resume, over-budget rejection and historical validation); operation_test passed 65 and keyboard_forge_test 364. No engine errors. Both required --refined historical behavior probes completed.

Level 1 --vanguard --soak completed in 1078.0 seconds with artificial health, final-boss arrival player level 20 and final player level 21. This is route/progression evidence, not a human win-rate or difficulty guarantee. No automated rewards persisted; no human game restarted. Logs under ignored output/easing-*. Handoff/dated feedback updated; release not rebuilt.


## 12 September 2026 - field pickups and visual audit

Current campaign has independent 45-75s schedules fulfilled on kills for health_pack and vacuum. Dedicated seeded RNG does not consume loot RNG. Health restores a quarter max HP; sweep snapshots existing XP/supplies and claims dropped chests. Supplies are marked spent before dispatch; additional magnets are consumed without recursion. Twenty-live-drop cap keeps health packs at 25% rather than aggregating potency. Legacy result wording now Run complete; current selector already said Levels. Internal identifiers unchanged.

scripts/check.ps1 passed all 41 suites, including 12 field-pickup checks for schedule range/independence, deadline fulfillment, no immediate repetition, healing cap and one-time claim, distant XP/supplies/chests, recursive-magnet prevention, repeated sweep idempotency and no Practice timed rewards. Both --refined historical probes completed without engine errors. Level 1 --vanguard --soak completed in 1052.7 seconds, final-boss arrival player level 20, final player level 21, peak 26 enemies, p95 simulation 810us. Artificial health means this cannot prove healing balance or human difficulty.

Fresh rendered field-pickup tests passed with normal/reduced icons inspected at 1600x900. Home and level/mastery/equipment/camp capture scripts ran without engine errors; final collection toast/audio integration checked through rendered startup. Audit documents empty-camp-fixture and static-image limitations. Existing icon source masters remain untouched; new graphics are native geometry. Output/field-* and audit captures are ignored. No player saves/rewards written, no game restarted, no release uploaded. Handoff and dated feedback updated.

## 12 September 2026 — foundry visual pass evidence

Scope: presentation-only FoundryTerrain/FoundryFrame, miniboss draw silhouettes/state motion, camp tabs and mastery casing/connections. No simulation behavior edits. See FOUNDRY_VISUAL_30.

- scripts/check.ps1: all 41 suites pass (output/foundry-check.log).
- Both mobabot09_behavior_probe.gd and mobabot10_behavior_probe.gd with --refined complete without logged engine errors (foundry-refined09/10.log).
- Current expedition_behavior_probe.gd --vanguard --soak: won at 1052.7 simulation seconds, level 21, 1609 kills, peak 26. Stage arrivals 300/624/955.1 seconds, levels 5/12/20, XP 92/225/392, credits 132/459/576. All identical to output/field-soak.log, including cast totals. Artificial health: not human balance evidence. Concurrent-load CPU timings are not performance acceptance.
- tests/foundry_visual_capture.gd: enemy/terrain data and loot RNG unchanged by rendering; normal/reduced, crowd and all four camp captures complete with empty final error log. Fixture saves are disabled. An initial typed-variable parse error was fixed; focus-loss pause capture corrected with auto_play; mastery header divider repositioned. Final captures inspected after these fixes.
- Paired 180-enemy renderer cadence: baseline 67d23bc median/p95 44455/50448 microseconds; current 48233/57458. Thirty measured samples each after ten warmup frames, same fixture and callback path. This includes engine/script/frame pacing, not isolated GPU cost. Approximately 3.8 ms median additional cost is recorded openly; target-machine crowded human review remains pending.
- No new bitmap provenance required; native geometry only, existing masters unchanged. No player game restarted, saves altered or release uploaded.

## 12 September 2026 — factory layouts

FACTORY_LEVELS_31 records scope, research, compatibility and reproducible evidence. scripts/check.ps1: 42 suites pass, including 151 factory checks. Coarse connectivity covers all nine level/round layouts; clear spawn, wall bounds, camera/player/RNG independence, saved map restoration, malformed flag rejection and Practice exclusion verified. The initial malformed-string engine error was fixed with an explicit type guard. Final rendered fixture and Vanguard test pass after minimap/reset additions. Both historical refined probes complete without logged errors.

Current --vanguard --soak Levels 1/2/3 finish at 1075.2/1092.2/1196.8 seconds, final levels21/23/21, kills1634/2488/2774, peak enemies28/99/175. Artificial health only; not human balance. Peak175 in Level3 warrants crowded human performance review. Local logs output/factory-check.log, factory-soak-1/2/3.log, factory-render.log and factory-*behavior_probe.log remain ignored. Three starting-area captures and selector inspected; no saves/rewards persisted, no player process restarted, no release uploaded.

## 12 September 2026 — Windows release validation

0.31.0-test.1 packaged from ee8db1575bffeffc0b724772222ee882e7d1f476. Full 42-suite regression passed; focused diagnostics/interface/HUD passed after release metadata fixes. Exported pack smoke verifies Practice, icons, nine music streams, project version and all three current factory layouts; actual release EXE rendered startup and exited 0. Initial smoke assertion assumed at least 12 props for every map, but Cooling has ten; corrected to the authoritative layout count and reran successfully. This was a test assumption error, not missing exported content. Source export unchanged by that test-only correction.

ZIP SHA256 B0CF0CFA74E8EC75A45151F58FEA15AFF90270C039513AF0AF1EDF1A0ABCD28C. Known weaknesses and human-review targets are in RELEASE_REVIEW_31. No permanent automated rewards, no existing game restarted. Publication confirmation follows in the stable handoff.

## 12 September 2026 — concise player-facing copy

Removed unsolicited route/timing/reset/persistence captions and repetitive build/upgrade footers. Full scripts/check.ps1 passes all 42 suites (output/copy32-check.log). Rendered selector inspected; isolated pause-menu verification confirms Main menu opens the leave dialog and Stay preserves the paused run, with persistence disabled (output/copy32-capture.log). Final compact button capture rerun. Existing saved checkpoints unchanged; no mid-combat save feature. Published 0.31.0-test.1 ZIP predates these edits.

## 12 September 2026 — shared foundry controls verification

scripts/check.ps1: all 42 suites pass (output/buttons33-check.log). New shared StyleBox rendering inspected for normal/hover/pressed/disabled/focus states; disabled text remains readable and focus brackets stay distinct from tab selection. Settings options/controls and all four camp screens rendered at 1600x900. Final mastery/camp captures inspected after casing/spacing adjustments. tests/foundry_controls_capture.gd also verifies reduced-effects toggle and Q casting control still change their intended setting with persist_settings=false. Final import/capture logs have no engine errors. Captures remain ignored under output/buttons33-*.png.

Native SVG fader and native control geometry only; no new generated raster art or changed pixelation pipeline. No continuous animations added; this does not measure overall combat performance. Existing Home plate art retained. No player session restarted, permanent rewards written or release replaced. Published ZIP remains 0.31.0-test.1 until a subsequent packaging pass.

## 12 September 2026 — equipment action availability

All 42 scripts/check.ps1 suites pass (output/gear34-check.log); level progression now has 58 checks including non-mutating availability, two/three copies, highest tier, full next-tier storage, best equipped, empty equipped slots and blocked profiles. Isolated equipment render confirms both gray controls have reasons, arrow cursors and re-enable with useful inventory changes. Injected viewport hover over disabled Auto craft produces the delayed custom tooltip; screenshot output/gear34-disabled.png inspected. Initial capture-script type inference error corrected before final render. No player inventory/preferences saved; forging/equipping transactions unchanged. ZIP not rebuilt.


## 12 September - Discovery35 verified candidate

- scripts/check.ps1: all43 suites passed; final focused discovery35_test.gd111 checks after strict malformed-threshold validation. Gameplay pause/clear-to-camp, bonus caps, credit duplicate protection, old progression, checkpoint round-trip, allnine coarse map connectivity checks, pressure-plate reachability, press damage and wall-safe cooling slow covered.
- Rendered selector, three maps and normal/reduced cards without persistence/audio. Retained source masters/provenance and256x384 pixelated variants. Corrected render-fixture typed-array assignment found by actual execution. Player game untouched.
- New Level1 artificial-health route: won1000.9s, guardian entry levels12/33, final boss40, peak21; Level3 won1098.1s, entry12/35/40, peak85. Final boss durations approx64.6/104s. No automated permanent rewards. Concurrent regressions mean CPU timings are not a clean performance comparison.
- Normal-health Level1 idle/active/basic policies lost27.8/146.3/132.9s at levels1/6/6. These limited bots do not prove owner difficulty; first specialist transition still merits attention.
- Both historical refined behavior probes completed with no engine errors. New behavior probe flag --discovery opts into the new rules while retaining historical comparison paths.
- New Windows release0.35.0-test.1 preparation authorized by the earlier distribution request. Export/startup/upload evidence will be appended after verification; do not equate source changes with the currently published ZIP.


### Discovery35 release verification

Published non-draft Windows pre-release v0.35.0-test.1 from commit49be864e21ad3fe4dffdfc27cb58602f4852cef3. ZIP102,706,296 bytes; local/GitHub SHA25629696f81ae456a54d84686ab062caf3e10a074199e1792690745b414667028ea. Checksum file uploaded. Matching Godot console engine loaded the exported PCK and passed both headless and rendered smoke, including Practice, all three illustrations, current progression/maps, icons and music. Release EXE started with a responsive MobaBot window. Its release template ignored external --script diagnostics, so that process was identified by exact executable path and stopped; no player process was stopped. Do not claim the EXE itself executed the scripted smoke. Source/art and release are on GitHub; follow-up documentation only.


Final correction: removed the legacy zero-chest count from current camp rewards. Published v0.35.0-test.2 from 25af5ac269abcbe2bc9a52608b2a349534a1518a; ZIP 102,706,330 bytes, SHA256 0b2d9f48e33dc27578bbeeeaadd33b86818e2fb1bcc5d9de3b3c7ecb1f70a5c6, matched by GitHub. UI audit: 4,294 labels, zero overflow; focused Discovery35: 111 passing checks. Refreshed PCK rendered smoke passes. Test.1/test.2 native EXEs have identical SHA256 03848543a6fa8618215fa7e03fefd96f509bfee81ac7e60f9be33207062bb1e5; startup evidence is retained, with the new PCK checked separately. No shared tag or published asset was overwritten.
